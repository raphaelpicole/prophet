{
  "version": 2,
  "buildCommand": "cd server && npm install && npm run build && mkdir -p ../api && mkdir -p ../src && cp -r dist/api/* ../api/ && cp -r dist/src/* ../src/",
  "outputDirectory": ".",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
