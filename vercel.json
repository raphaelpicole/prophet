{
  "version": 2,
  "installCommand": "cd server && npm install",
  "buildCommand": "echo 'No build needed - using prebuilt assets'",
  "outputDirectory": "app/build/web",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ],
  "crons": [{
    "path": "/api/cron/collect",
    "schedule": "0 11 * * *"
  }]
}